# Claude Code worktrees — isolated agent copies, never commit
.claude/worktrees/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.expo/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Python bytecode (sdk-python)
__pycache__/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Local-only docs and specs (PRS public-repo rule: docs and specs live locally)
*.md
*.markdown
specs/
spec/
docs/
design/
plans/

# Claude Code local context (per-user / per-project, not public)
CLAUDE.md
.claude/

# Internal design v2 mockups and screenshots
design-spec-v2/

# MCP browser-session logs and root-level screenshots (public repo: keep these
# out, they are one `git add .` from a public commit). Anchored to root so
# public/**/*.png|jpg served assets stay tracked.
.playwright-mcp/
/*.png
/*.jpeg
/*.jpg

# Local planning artifacts that aren't tracked
agent-outreach-kit.md
david-followup-*.md
mcp-submissions.md
server.json

# Unused / draft embed snippets (not deployed)
newsletter-embed-snippet.html
