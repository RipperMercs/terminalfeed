# Cloudflare Pages edge headers.
# Worker-served /api/* responses set their own headers in worker-additions/worker.js
# (see SECURITY_HEADERS const). Keep the two surfaces in lockstep.

/*
  Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
  X-Frame-Options: SAMEORIGIN
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=(), usb=(), magnetometer=(), gyroscope=(), interest-cohort=()
  X-XSS-Protection: 0
  Cross-Origin-Opener-Policy: same-origin-allow-popups
  Cross-Origin-Resource-Policy: cross-origin

# Embeddable widgets are explicitly designed to be iframed by other sites.
# Cover both the existing /embed/* directory (BTC price, fear-greed, etc.)
# and the future /widgets/* surface.
#
# Cloudflare Pages _headers is additive across matching rules — there is no
# "exclude" syntax — so the /* SAMEORIGIN above will still appear on embed
# paths. We rely on the CSP frame-ancestors directive which, per W3C spec,
# obsoletes and overrides X-Frame-Options when both are present. End result:
# browsers honor frame-ancestors * and let the embed render in any iframe.
/embed/*
  Content-Security-Policy: frame-ancestors *

/widgets/*
  Content-Security-Policy: frame-ancestors *
